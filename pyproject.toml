[project]
name = "breast-cancer-course"
version = "0.1.0"
description = "Cours Deep Learning sur mammographies (RSNA) — 8 chapitres notebooks (PyTorch → risques garantis)."
requires-python = ">=3.10"

# torch / torchvision / torchaudio sont fournis par l'image de base
# pytorch/pytorch:2.4.1-cuda12.1-cudnn9-runtime -> NE PAS les lister ici
# (uv les réinstallerait, conflit + téléchargement CUDA inutile).
dependencies = [
    # --- Jupyter ---
    "jupyterlab>=4.0",
    "ipywidgets>=8.0",

    # --- Stack scientifique (ch 2 à 8) ---
    "numpy",
    "pandas",
    "scipy",
    "scikit-learn",
    "matplotlib",
    "tqdm",

    # --- Image / DICOM, pré-traitement (ch 3) ---
    "opencv-python-headless",
    "imageio",
    "h5py",
    "pydicom==3.0.2",
    "pylibjpeg==2.1.0",
    "pylibjpeg-libjpeg",
    "pylibjpeg-openjpeg",

    # (Plus de kornia : la normalisation GPU du ch5 se fait avec de simples
    #  opérations tensorielles torch — x.mean/x.std sur le batch. Rien à installer en plus.)

    # --- Téléchargement des jeux de données (ch 1) ---
    # kaggle récent (>=1.7) : les tokens d'API actuels exigent une version 2.x-compatible.
    "kaggle",
    "kagglehub",
]
